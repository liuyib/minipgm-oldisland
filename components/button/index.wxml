<!--components/button/index.wxml-->
<button class="container" open-type="{{ openType }}" bind:getuserinfo="onGetUserInfo">
  <block wx:if="{{ !isSlot }}">
    {{ text }}
  </block>

  <slot wx:if="{{ isSlot }}" name="image"></slot>
</button>
