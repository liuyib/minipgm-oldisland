<!--components/button/index.wxml-->
<button class="container ext-class" open-type="{{ openType }}" bind:getuserinfo="onGetUserInfo">
  <block wx:if="{{ !isSlot }}">
    {{ text }}
  </block>

  <slot wx:if="{{ isSlot }}" name="image"></slot>
</button>
