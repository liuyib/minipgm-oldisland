<!--components/like/index.wxml-->
<view class="like-container" bind:tap="onLike">
  <image class="like-image" src="{{ isLike ? likeSrc : dissSrc }}"></image>
  <text class="like-nums">{{ count }}</text>
</view>
